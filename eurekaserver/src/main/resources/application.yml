server:
  port: 8761
eureka:
  client:
    #表示是否注册自己（注册中心本身就是一个微服务）
    register-with-eureka: false
    #是否同步其他注册中心的数据
    fetch-registry: false
    #访问注册中心的路径
    service-url:
      defaultZone: http://localhost:8761/eureka/
